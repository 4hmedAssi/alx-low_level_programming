#include <stdio.h>
/**
 * main - it is a functionon have puts and return
 * puts : prints function
 * Return : 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}

